% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-compare.R
\name{surf_compare}
\alias{surf_compare}
\title{Compare two surfaces side-by-side or as a difference surface}
\usage{
surf_compare(
  object1,
  object2,
  type = c("difference", "side_by_side"),
  labels = list()
)
}
\arguments{
\item{object1}{An \link[=new_effectsurf]{effectsurf} object.}

\item{object2}{An \link[=new_effectsurf]{effectsurf} object with the same
x and y variables and grid resolution.}

\item{type}{Character. \code{"difference"} (default) computes
\code{object1 - object2} as a new surface. \code{"side_by_side"} returns
both objects in a list for manual comparison.}

\item{labels}{Named list. Custom labels for the difference surface.}
}
\value{
For \code{"difference"}: a new \code{effectsurf} object. For
\code{"side_by_side"}: a named list with elements \code{surface1} and \code{surface2}.
}
\description{
Computes the difference between two \code{effectsurf} objects or creates
a side-by-side comparison. Useful for comparing models (e.g.,
DAG-informed vs legacy), or comparing two different treatments.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  m1 <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  m2 <- mgcv::gam(mpg ~ s(wt) + hp, data = mtcars)

  es1 <- surf_prediction(m1, x = "wt", y = "hp",
                         x_length = 20, y_length = 20)
  es2 <- surf_prediction(m2, x = "wt", y = "hp",
                         x_length = 20, y_length = 20)

  diff_surf <- surf_compare(es1, es2, type = "difference")
  plot(diff_surf)
}
}
}
