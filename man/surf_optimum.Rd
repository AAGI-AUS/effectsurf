% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-optimum.R
\name{surf_optimum}
\alias{surf_optimum}
\title{Find the optimum (maximum or minimum) of a surface}
\usage{
surf_optimum(object, type = c("max", "min"))
}
\arguments{
\item{object}{An \link[=new_effectsurf]{effectsurf} object.}

\item{type}{Character. \code{"max"} (default) or \code{"min"}.}
}
\value{
A \code{data.table} with columns: \code{stratum} (if stratified), the
x variable, the y variable, \code{estimate}, and optionally \code{conf.low},
\code{conf.high}.
}
\description{
Locates the combination of x and y values that maximises or minimises
the predicted response on the surface grid.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 30, y_length = 30)
  surf_optimum(es, type = "max")
}
}
}
