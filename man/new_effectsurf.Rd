% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effectsurf-class.R
\name{new_effectsurf}
\alias{new_effectsurf}
\title{Create an effectsurf object}
\usage{
new_effectsurf(
  data,
  x_var,
  y_var,
  z_var,
  strata_var = NULL,
  type = c("prediction", "comparison", "slopes", "cate"),
  ci = FALSE,
  model_info = list(),
  labels = list(),
  transform = NULL,
  meta = list()
)
}
\arguments{
\item{data}{A \code{data.table} containing the prediction grid with columns for
the x variable, y variable, \code{estimate}, and optionally \code{std.error},
\code{conf.low}, \code{conf.high}, and the stratification variable.}

\item{x_var}{Character. Name of the x-axis variable.}

\item{y_var}{Character. Name of the y-axis variable.}

\item{z_var}{Character. Name of the response (z-axis) variable.}

\item{strata_var}{Character or \code{NULL}. Name of the categorical
stratification variable.}

\item{type}{Character. One of \code{"prediction"}, \code{"comparison"}, \code{"slopes"},
\code{"cate"}.}

\item{ci}{Logical. Whether confidence intervals are included.}

\item{model_info}{A named list with model metadata: \code{class}, \code{formula},
\code{n_obs}, \code{call}.}

\item{labels}{A named list with display labels: \code{x}, \code{y}, \code{z}, \code{title}.}

\item{transform}{Character or function or \code{NULL}. Back-transformation to
apply for display (e.g., \code{"expit"} for logit models).}

\item{meta}{A named list with additional metadata.}
}
\value{
An object of class \code{effectsurf}.
}
\description{
Constructor for the \code{effectsurf} S3 class. Not typically called directly
by users; instead, use \code{\link[=surf_prediction]{surf_prediction()}}, \code{\link[=surf_comparison]{surf_comparison()}},
\code{\link[=surf_slopes]{surf_slopes()}}, or \code{\link[=surf_cate]{surf_cate()}}.
}
\examples{
# Typically created via surf_prediction(), not directly
es <- new_effectsurf(
  data = data.table::data.table(
    x = rep(1:3, each = 3), y = rep(1:3, 3),
    estimate = rnorm(9)
  ),
  x_var = "x", y_var = "y", z_var = "response",
  type = "prediction"
)
print(es)
}
