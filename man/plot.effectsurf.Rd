% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render-surface.R
\name{plot.effectsurf}
\alias{plot.effectsurf}
\title{Plot an effectsurf object as an interactive 3D surface}
\usage{
\method{plot}{effectsurf}(
  x,
  opacity = 0.85,
  colourscale = "Viridis",
  show_ci = FALSE,
  ci_opacity = 0.3,
  wireframe = FALSE,
  show_data = FALSE,
  camera = NULL,
  save_html = NULL,
  fmt = NULL,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{An \link[=new_effectsurf]{effectsurf} object.}

\item{opacity}{Numeric (0-1). Surface opacity. Default \code{0.85}.}

\item{colourscale}{Character or list. Plotly colourscale name
(e.g., \code{"Viridis"}, \code{"RdBu"}, \code{"Blues"}) or a custom colourscale list.
For stratified surfaces, a vector of colours is used instead.}

\item{show_ci}{Logical. Whether to show confidence interval surfaces
(upper/lower bounds as translucent surfaces). Default \code{FALSE}.}

\item{ci_opacity}{Numeric (0-1). Opacity for CI surfaces. Default \code{0.3}.}

\item{wireframe}{Logical. If \code{TRUE}, render surfaces as wireframes instead
of filled surfaces. Default \code{FALSE}.}

\item{show_data}{Logical. If \code{TRUE} and the model's training data is
available, overlay observed data points. Default \code{FALSE}.}

\item{camera}{A list specifying camera position, e.g.,
\code{list(eye = list(x = 1.5, y = 1.5, z = 1.2))}.}

\item{save_html}{Character or \code{NULL}. If a file path is provided,
saves the interactive plot as a self-contained HTML file.
When saving, 2D maximum-profile plots (PDF) are also generated
alongside the HTML, matching the prototype workflow.
Default \code{NULL} (no file saved).}

\item{fmt}{A \code{\link[=label_format]{label_format()}} object controlling number formatting
in hover text and axes. Default \code{NULL} (auto-format).}

\item{width, height}{Plot dimensions in pixels. \code{NULL} for auto-sizing.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{plotly} htmlwidget object (invisibly if \code{save_html} is used).
}
\description{
Renders an \code{effectsurf} object as an interactive Plotly 3D surface plot.
When the object is stratified by a categorical variable, overlaid surfaces
are produced with independent colourscales and legend toggles.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 25, y_length = 25)
  plot(es)
  plot(es, opacity = 0.9, colourscale = "RdBu")

  # Save to HTML (also produces 2D profile PDFs)
  plot(es, save_html = tempfile(fileext = ".html"))
}
}
}
\seealso{
\code{\link[=surf_prediction]{surf_prediction()}}, \code{\link[=surf_export]{surf_export()}}, \code{\link[=surf_profile]{surf_profile()}},
\code{\link[=label_format]{label_format()}}
}
