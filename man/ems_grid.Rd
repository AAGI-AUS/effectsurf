% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems-grid.R
\name{ems_grid}
\alias{ems_grid}
\title{Create a prediction grid for surface estimation}
\usage{
ems_grid(
  model,
  x,
  y,
  by = NULL,
  x_range = NULL,
  y_range = NULL,
  x_length = 50L,
  y_length = 50L,
  grid_type = "mean_or_mode",
  at = NULL,
  levels_needed = NULL,
  method = c("marginaleffects", "emmeans", "gratia", "manual"),
  ...
)
}
\arguments{
\item{model}{A fitted model object.}

\item{x}{Character. Name of the x-axis variable.}

\item{y}{Character. Name of the y-axis variable.}

\item{by}{Character or \code{NULL}. Name of a categorical variable for
stratification.}

\item{x_range}{Numeric vector of length 2. Range for x variable. If \code{NULL},
auto-detected from the model's training data.}

\item{y_range}{Numeric vector of length 2. Range for y variable. If \code{NULL},
auto-detected.}

\item{x_length}{Integer. Number of grid points along x. Default 50.}

\item{y_length}{Integer. Number of grid points along y. Default 50.}

\item{grid_type}{Character. One of \code{"mean_or_mode"} (default),
\code{"counterfactual"}, \code{"balanced"}. Passed to
\code{\link[marginaleffects:datagrid]{marginaleffects::datagrid()}}.}

\item{at}{Named list. Additional variables to hold at specific values
(e.g., \code{at = list(state = "WA")}).}

\item{levels_needed}{Character vector or \code{NULL}. If \code{by} is specified, subset
to only these levels.}

\item{method}{Character. One of \code{"marginaleffects"} (default), \code{"gratia"},
or \code{"manual"}. Controls the grid creation backend.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
A \code{data.table} representing the prediction grid.
}
\description{
Generates a cross-product grid of two focal continuous variables, with
non-focal variables held at reference values (mean for numeric, mode for
factors). Wraps \code{\link[marginaleffects:datagrid]{marginaleffects::datagrid()}} when available, with a
lightweight fallback for unsupported models.
}
\examples{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp) + cyl, data = mtcars)
  grid <- ems_grid(model, x = "wt", y = "hp", x_length = 20, y_length = 20)
  head(grid)
}
}
