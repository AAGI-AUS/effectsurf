% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-cate.R
\name{surf_cate}
\alias{surf_cate}
\title{Create a 3D conditional average treatment effect (CATE) surface}
\usage{
surf_cate(
  model,
  x,
  y,
  x_range = NULL,
  y_range = NULL,
  x_length = 50L,
  y_length = 50L,
  predict_fun = NULL,
  ci = FALSE,
  training_data = NULL,
  at = NULL,
  smooth = NULL,
  transform = NULL,
  labels = list(),
  ...
)
}
\arguments{
\item{model}{A fitted causal model (e.g., from \code{grf::causal_forest()}).}

\item{x}{Character. Name of the x-axis moderator variable.}

\item{y}{Character. Name of the y-axis moderator variable.}

\item{x_range, y_range}{Numeric vectors of length 2.}

\item{x_length, y_length}{Integer. Grid resolution.}

\item{predict_fun}{A function with signature \verb{function(model, newdata)}
returning a numeric vector of CATE estimates. For \code{grf::causal_forest},
this would be \code{function(m, d) predict(m, d)$predictions}.}

\item{ci}{Logical. Whether to include confidence intervals. Requires
\code{predict_fun} to return a data.frame with \code{estimate}, \code{conf.low},
\code{conf.high} columns.}

\item{training_data}{A \code{data.frame} of the original training data. Needed
for determining non-focal variable reference values when the model
object does not store training data.}

\item{at}{Named list of fixed variable values.}

\item{smooth}{Post-prediction surface smoothing via \code{\link[mgcv:gam]{mgcv::gam()}}.
See \code{\link[=surf_prediction]{surf_prediction()}} for full documentation. \code{NULL} (default) = no
smoothing; \code{TRUE} = auto-smooth; or a named list with \code{k}, \code{bs},
\code{smooth_ci}.}

\item{transform}{Back-transformation specification.}

\item{labels}{Custom axis labels.}

\item{...}{Additional arguments passed to \code{predict_fun}.}
}
\value{
An \link[=new_effectsurf]{effectsurf} object of type \code{"cate"}.
}
\description{
Generates a surface of treatment effect heterogeneity from causal
inference models (e.g., causal forests from \code{grf}, or custom CATE
estimators). Requires a custom \code{predict_fun} that returns treatment
effect estimates for each observation.
}
\details{
For standard regression models, use \code{\link[=surf_comparison]{surf_comparison()}} instead —
it provides CATE-like surfaces via \code{marginaleffects::comparisons()}.
}
\examples{
\donttest{
# Example with a simple model (grf requires separate installation)
# library(grf)
# cf <- causal_forest(X, Y, W)
# es <- surf_cate(cf, x = "age", y = "income",
#                 predict_fun = function(m, d) predict(m, d)$predictions,
#                 training_data = X)
# plot(es)
}
}
\seealso{
\code{\link[=surf_comparison]{surf_comparison()}}, \code{\link[=surf_prediction]{surf_prediction()}}
}
