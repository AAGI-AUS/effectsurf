% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-contour.R
\name{surf_contour}
\alias{surf_contour}
\title{Create a 2D contour/heatmap from a 3D surface}
\usage{
surf_contour(
  object,
  type = c("contour", "heatmap"),
  nlevels = 10L,
  colourscale = "Viridis",
  interactive = TRUE,
  ...
)
}
\arguments{
\item{object}{An \link[=new_effectsurf]{effectsurf} object.}

\item{type}{Character. \code{"contour"} (default) for contour lines,
\code{"heatmap"} for a filled heatmap.}

\item{nlevels}{Integer. Number of contour levels. Default 10.}

\item{colourscale}{Character. Colour palette name for fill. Default
\code{"Viridis"}.}

\item{interactive}{Logical. If \code{TRUE} (default), returns an interactive
Plotly widget. If \code{FALSE} and \code{ggplot2} is available, returns a ggplot.}

\item{...}{Additional arguments passed to Plotly or ggplot functions.}
}
\value{
A \code{plotly} widget or \code{ggplot} object.
}
\description{
Projects the 3D prediction surface into a 2D contour plot or filled
heatmap. Useful for publication-ready static figures.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 30, y_length = 30)
  surf_contour(es, type = "heatmap")
}
}
}
