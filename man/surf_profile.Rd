% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-profile.R
\name{surf_profile}
\alias{surf_profile}
\title{Extract 2D profile slices from a 3D surface}
\usage{
surf_profile(object, along = c("x", "y"), at = NULL, use_ggplot = TRUE, ...)
}
\arguments{
\item{object}{An \link[=new_effectsurf]{effectsurf} object.}

\item{along}{Character. Which axis to plot along: \code{"x"} (default) or \code{"y"}.
\itemize{
\item \code{"x"}: fix y at specific values, plot response vs x.
\item \code{"y"}: fix x at specific values, plot response vs y.
}}

\item{at}{Numeric vector or \code{NULL}. Values at which to fix the other
variable. If \code{NULL}, uses 5 evenly spaced quantile values.}

\item{use_ggplot}{Logical. If \code{TRUE} and \code{ggplot2} is available, returns
a ggplot2 object. If \code{FALSE} (default), returns a list of data and
a base R plot.}

\item{...}{Additional arguments passed to plotting functions.}
}
\value{
If \code{use_ggplot = TRUE}, a \code{ggplot} object. Otherwise, a named list
with \code{data} (a data.table of profile slices) and \code{plot} (called for
side effect).
}
\description{
Projects the 3D surface into 2D by fixing one variable at specific values
and plotting the response against the other variable. This bridges
interactive 3D exploration with publication-ready 2D figures.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 30, y_length = 30)

  # Profile along x at 5 values of y
  surf_profile(es, along = "x")

  # Profile along y at specific x values
  surf_profile(es, along = "y", at = c(2, 3, 4, 5))
}
}
}
