% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-slopes.R
\name{surf_slopes}
\alias{surf_slopes}
\title{Create a 3D marginal effect (slope) surface}
\usage{
surf_slopes(
  model,
  x,
  y,
  variable,
  slope = "dydx",
  by = NULL,
  x_range = NULL,
  y_range = NULL,
  x_length = 50L,
  y_length = 50L,
  ci = TRUE,
  level = 0.95,
  grid_type = "mean_or_mode",
  at = NULL,
  levels_needed = NULL,
  transform = NULL,
  labels = list(),
  smooth = NULL,
  method = c("marginaleffects", "emmeans", "manual"),
  ...
)
}
\arguments{
\item{model}{A fitted model object. Supports 100+ model classes via
\code{marginaleffects}, including \code{lm}, \code{glm}, \code{gam}, \code{bam}, \code{lmer},
\code{glmer}, \code{brm}, and many more.}

\item{x}{Character. Name of the x-axis continuous variable.}

\item{y}{Character. Name of the y-axis continuous variable.}

\item{variable}{Character. The focal variable whose marginal effect
(slope) is computed.}

\item{slope}{Character. Type of slope: \code{"dydx"} (default), \code{"eyex"},
\code{"eydx"}, \code{"dyex"}. See \code{\link[marginaleffects:slopes]{marginaleffects::slopes()}}.}

\item{by}{Character or \code{NULL}. Name of a categorical variable for
stratification. Each level produces a separate surface.}

\item{x_range}{Numeric vector of length 2. Range \code{c(min, max)} for x.
If \code{NULL}, auto-detected from the model's training data.}

\item{y_range}{Numeric vector of length 2. Range \code{c(min, max)} for y.
If \code{NULL}, auto-detected.}

\item{x_length}{Integer. Number of grid points along x. Default 50.}

\item{y_length}{Integer. Number of grid points along y. Default 50.}

\item{ci}{Logical. Whether to compute confidence intervals. Default \code{TRUE}.}

\item{level}{Numeric. Confidence level. Default \code{0.95}.}

\item{grid_type}{Character. How to handle non-focal variables. One of
\code{"mean_or_mode"} (default) or \code{"counterfactual"}. See
\code{\link[marginaleffects:datagrid]{marginaleffects::datagrid()}} for details.}

\item{at}{Named list. Fix specific variables at given values
(e.g., \code{at = list(state = "WA", nitrogen = 80)}).}

\item{levels_needed}{Character vector or \code{NULL}. Subset the \code{by} variable
to specific levels.}

\item{transform}{Character, function, or \code{NULL}. Back-transformation for
the response (e.g., \code{"expit"} for logit-scale models, \code{"exp"} for
log-scale). Built-in options: \code{"expit"}, \code{"exp"}, \code{"square"},
\code{"percent_expit"}. Or supply a custom function.}

\item{labels}{Named list with custom axis labels: \code{x}, \code{y}, \code{z}, \code{title}.}

\item{smooth}{Post-prediction surface smoothing via \code{\link[mgcv:gam]{mgcv::gam()}}.
Useful for non-smooth models (random forests, boosted trees, MARS) where
raw predictions form jagged or step-function surfaces. A tensor product
smooth \code{te(x, y)} is fitted to the predicted values as a visual
approximation. Options:
\itemize{
\item \code{NULL} (default): No smoothing -- raw predictions displayed.
\item \code{TRUE}: Auto-smooth with \code{te(x, y, k = -1, bs = "tp")}, letting
\code{mgcv} auto-select the basis dimension (same default as \code{\link[mgcv:s]{mgcv::s()}}).
\item A named list for manual control:
\itemize{
\item \code{k}: Basis dimension per marginal (default \code{-1} = auto).
Higher values preserve more detail; lower values smooth more.
\item \code{bs}: Basis type (default \code{"tp"}). See \code{\link[mgcv:te]{mgcv::te()}}.
\item \code{smooth_ci}: Logical. Also smooth confidence intervals?
Default \code{TRUE}.
When strata are present (\code{by} is set), a separate smooth is fitted
per stratum, preserving interaction structure across groups.
Requires the \code{mgcv} package.
}
}}

\item{method}{Character. Computation backend: \code{"marginaleffects"} (default),
\code{"gratia"} (for mgcv models), or \code{"manual"} (fallback).}

\item{...}{Additional arguments passed to the prediction backend.}
}
\value{
An \link[=new_effectsurf]{effectsurf} object of type \code{"slopes"}.
}
\description{
Generates a surface showing how the marginal effect (partial derivative)
of a focal variable varies across a 2D grid of two moderators.
Uses \code{\link[marginaleffects:slopes]{marginaleffects::slopes()}} as the computation backend.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp) + s(disp), data = mtcars)

  # How does the marginal effect of wt change across hp x disp?
  es <- surf_slopes(model, x = "hp", y = "disp",
                    variable = "wt",
                    x_length = 25, y_length = 25)
  plot(es)
}
}
}
\seealso{
\code{\link[=surf_prediction]{surf_prediction()}}, \code{\link[=surf_comparison]{surf_comparison()}}, \code{\link[=surf_sensitivity]{surf_sensitivity()}}
}
