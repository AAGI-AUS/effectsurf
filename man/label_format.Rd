% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label-format.R
\name{label_format}
\alias{label_format}
\title{Create a label format specification}
\usage{
label_format(
  x = NULL,
  y = NULL,
  z = NULL,
  hover_x = NULL,
  hover_y = NULL,
  hover_z = NULL,
  digits = 2L
)
}
\arguments{
\item{x}{Format for x-axis values. A \code{sprintf} format string
(e.g., \code{"\%.0f kg/ha"}), a function, or \code{NULL} (auto).}

\item{y}{Format for y-axis values. Same as \code{x}.}

\item{z}{Format for z-axis values. Same as \code{x}.}

\item{hover_x}{Format for x values in hover text. Defaults to \code{x}.}

\item{hover_y}{Format for y values in hover text. Defaults to \code{y}.}

\item{hover_z}{Format for z values in hover text. Defaults to \code{z}.}

\item{digits}{Integer. Default number of decimal places when format
is \code{NULL}. Default 2.}
}
\value{
A \code{label_format} list used by \code{\link[=plot.effectsurf]{plot.effectsurf()}}.
}
\description{
Defines how numeric values are formatted in axis labels, hover text,
colorbar, and legend. Accepts \code{sprintf}-style format strings, custom
functions, or named shortcuts.
}
\examples{
# Format nitrogen as integer with unit, rainfall with no decimals
fmt <- label_format(
  x = "\%.0f kg/ha",
  y = "\%.0f mm",
  z = "\%.2f t/ha"
)

# Custom function
fmt2 <- label_format(
  z = function(v) paste0(round(v * 100, 1), "\%")
)
}
