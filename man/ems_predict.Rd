% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems-predict.R
\name{ems_predict}
\alias{ems_predict}
\title{Generate predictions on a grid}
\usage{
ems_predict(
  model,
  grid,
  ci = TRUE,
  level = 0.95,
  vcov = NULL,
  predict_fun = NULL,
  method = c("marginaleffects", "emmeans", "gratia", "predict"),
  ...
)
}
\arguments{
\item{model}{A fitted model object.}

\item{grid}{A \code{data.frame} or \code{data.table} prediction grid, typically
produced by \code{\link[=ems_grid]{ems_grid()}}.}

\item{ci}{Logical. Whether to compute confidence intervals. Default \code{TRUE}.}

\item{level}{Numeric. Confidence level for intervals. Default \code{0.95}.}

\item{vcov}{Variance-covariance specification passed to
\code{\link[marginaleffects:predictions]{marginaleffects::predictions()}}. Use \code{FALSE} for fast predictions
without standard errors.}

\item{predict_fun}{A custom prediction function with signature
\verb{function(model, newdata)} returning a numeric vector. If supplied,
bypasses both \code{marginaleffects} and \code{predict()}.}

\item{method}{Character. One of \code{"marginaleffects"} (default), \code{"gratia"},
or \code{"predict"}.}

\item{...}{Additional arguments passed to the prediction backend.}
}
\value{
A \code{data.table} with the grid columns plus \code{estimate},
and optionally \code{std.error}, \code{conf.low}, \code{conf.high}.
}
\description{
Computes model predictions (with optional confidence intervals) on a
prediction grid. Uses \code{\link[marginaleffects:predictions]{marginaleffects::predictions()}} as the primary
backend, with a fallback to \code{\link[stats:predict]{stats::predict()}}.
}
