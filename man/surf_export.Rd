% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-export.R
\name{surf_export}
\alias{surf_export}
\title{Export an effectsurf plot to HTML}
\usage{
surf_export(object, path, selfcontained = TRUE, title = NULL, ...)
}
\arguments{
\item{object}{An \link[=new_effectsurf]{effectsurf} object.}

\item{path}{Character. File path for the output HTML. Must end in \code{.html}.}

\item{selfcontained}{Logical. If \code{TRUE} (default), produces a single
self-contained HTML file (~3.8 MB, fully portable).}

\item{title}{Character or \code{NULL}. HTML page title (currently unused,
title comes from the effectsurf object).}

\item{...}{Additional arguments passed to \code{\link[=plot.effectsurf]{plot.effectsurf()}} for
controlling the visualisation appearance.}
}
\value{
Invisibly returns the file path.
}
\description{
Saves an interactive 3D surface visualisation as a self-contained HTML
file that can be opened in any web browser, shared, or embedded in
reports. Also generates 2D maximum-profile PDFs alongside the HTML.
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 20, y_length = 20)
  # Export to temp file
  tmp <- tempfile(fileext = ".html")
  surf_export(es, path = tmp)
}
}
}
