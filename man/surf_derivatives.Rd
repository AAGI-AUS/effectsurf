% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surf-derivatives.R
\name{surf_derivatives}
\alias{surf_derivatives}
\title{Compute derivative surfaces from an effectsurf object}
\usage{
surf_derivatives(object, order = 2L, type = "all", sigdigits = 10L)
}
\arguments{
\item{object}{An \link[=new_effectsurf]{effectsurf} object (typically from
\code{\link[=surf_prediction]{surf_prediction()}}).}

\item{order}{Integer. \code{1} for first-order derivatives only (gradient),
\code{2} for both first- and second-order (curvature + interaction).
Default \code{2}.}

\item{type}{Character vector specifying which derivatives to compute.
Default \code{"all"}. Options:
\describe{
\item{\code{"dzdx"}}{Partial derivative w.r.t. x (dz/dx)}
\item{\code{"dzdy"}}{Partial derivative w.r.t. y (dz/dy)}
\item{\code{"gradient"}}{Gradient magnitude sqrt((dz/dx)^2 + (dz/dy)^2)}
\item{\code{"d2zdx2"}}{Second partial w.r.t. x (d^2z/dx^2) -- curvature in x}
\item{\code{"d2zdy2"}}{Second partial w.r.t. y (d^2z/dy^2) -- curvature in y}
\item{\code{"d2zdxdy"}}{Cross-partial (d^2z/dxdy) -- local interaction strength}
\item{\code{"all"}}{All of the above (filtered by \code{order})}
}}

\item{sigdigits}{Integer. Noise suppression threshold. Derivative values
smaller than \verb{10^(-sigdigits) * max(|z|)} are set to exactly zero.
This removes floating-point artefacts (e.g., \code{1e-13} instead of \code{0})
that arise when finite differences are applied to flat or near-linear
surfaces. Default \code{10} -- conservative, only removes values below
10 orders of magnitude of the surface range. Set to \code{NULL} to disable.}
}
\value{
A named list of \code{effectsurf} objects, one per requested derivative.
Each can be passed to \code{\link[=plot.effectsurf]{plot.effectsurf()}}, \code{\link[=surf_contour]{surf_contour()}},
\code{\link[=surf_export]{surf_export()}}, \code{\link[=surf_profile]{surf_profile()}}, etc.
}
\description{
Calculates first-order (gradient) and second-order (curvature, interaction)
derivative surfaces using central finite differences on the prediction grid.
Each derivative is returned as a standard \code{effectsurf} object that can be
plotted, exported, and analysed with all existing package functions.
}
\details{
\strong{Finite difference scheme:} Central differences are used for interior
points; forward/backward differences at boundaries. The grid spacing
is determined from the effectsurf object's x and y sequences.

\strong{Scientific interpretation:}
\itemize{
\item \strong{dz/dx, dz/dy}: Local sensitivity of outcome to each predictor.
Regions where the gradient is near zero indicate plateaus or optima.
\item \strong{Gradient magnitude}: Identifies regions of rapid vs stable response.
\item \strong{d^2z/dx^2, d^2z/dy^2}: Curvature. Negative = concave (diminishing
returns); positive = convex (accelerating response). Zero-crossings
locate inflection points.
\item \strong{d^2z/dxdy}: Spatially-resolved interaction strength. Non-zero
values indicate that the effect of x depends on the level of y (and
vice versa) at that specific grid location.
}
}
\examples{
\donttest{
if (requireNamespace("mgcv", quietly = TRUE)) {
  model <- mgcv::gam(mpg ~ s(wt) + s(hp), data = mtcars)
  es <- surf_prediction(model, x = "wt", y = "hp",
                        x_length = 30, y_length = 30)

  derivs <- surf_derivatives(es)
  plot(derivs$dzdx)          # Sensitivity to wt
  plot(derivs$gradient)      # Overall sensitivity map
  plot(derivs$d2zdxdy)       # Interaction surface
}
}
}
